apiVersion: dapr.io/v1alpha1
kind: Component
metadata:
  name: pubsub
  namespace: dapr-go-hero-inventory
spec:
  type: pubsub.redis
  version: v1
  metadata:
    - name: redisHost
      value: "redis.dapr-go-hero.svc.cluster.local:6379"
    - name: redisPassword
      value: ""
    # At-least-once redelivery tuning (Redis Streams, consumer groups).
    # processingTimeout: how long a claimed message may be held before
    # Dapr assumes the subscriber died and re-delivers via XPENDING.
    # Tight (10s) so the e2e resiliency test has a deterministic SLO —
    # a miss within the 90s poll window is a real regression, not timing
    # noise. Raise for production workloads with longer handler runtimes.
    - name: processingTimeout
      value: "10s"
    # redeliverInterval: frequency of scanning pending entries for
    # messages that exceeded processingTimeout.
    - name: redeliverInterval
      value: "5s"
    - name: queueDepth
      value: "100"
